<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <h5 class="mb-3">Hey! you can edit your post here</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          placeholder="Your Thoughts?..write it here!"
          class="w-1/3 p-3 resize-none block outline-none bg-transparent border-2 border-zinc-800 rounded-md"
          name="content"
        >
<%= post.content %></textarea
        >
        <input
          class="px-3 w-40 py-2 bg-yellow-500 text-black rounded-md block mt-3"
          type="submit"
          Value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
